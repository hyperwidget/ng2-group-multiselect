import {
  beforeEach,
  describe,
  expect,
  it,
} from '@angular/core/testing';

import { MultiSelectComponent } from './index';

describe('Moderate Side Nav: Component', () => {
  let component: MultiSelectComponent;

  it('should run!', () => {
    expect(1).toBe(1);
  });

});